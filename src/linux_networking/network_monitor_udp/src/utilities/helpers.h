#ifndef __HELPERS_H__
#define __HELPERS_H__

#ifdef __cplusplus
extern "C"
{
#endif

  double gettime();

//#define max(x,y) ((x) > (y) ? (x) : (y))
//#define min(x,y) ((x) < (y) ? (x) : (y))

#ifdef __cplusplus
}
#endif

#endif
