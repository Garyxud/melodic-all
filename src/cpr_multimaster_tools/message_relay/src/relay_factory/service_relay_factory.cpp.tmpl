// Generated by message_relay for creating service relays.
// DO NOT EDIT

\#include "message_relay/relay_factory/service_relay_factory.h"

// Include generated message processors
#for $pkg_name in $pkg_names
\#include "message_relay/processor/$(pkg_name)_message_processor.h"
#end for

namespace message_relay
{

ServiceRelay::Ptr createServiceRelay(const ServiceRelayParams &params)
{

  ServiceRelay::Ptr relay;
  if (params.type.empty())
  {
    ROS_ERROR_STREAM("Must provide a service relay type for service " << params.service);
  }
  #for $srv_name in $msg_srv_names
  else if (params.type == "$(srv_name)")
  {
    relay = ServiceRelay::Ptr(new ServiceRelayImpl<$(srv_name.replace('/', '::'))>(params));
  }
  #end for
  else
  {
    ROS_ERROR_STREAM("Unsupported service relay type " << params.type);
  }
  return relay;
}

}  // namespace message_relay
