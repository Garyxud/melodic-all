// Generated by message_relay for creating topic relays.
// DO NOT EDIT

\#include "message_relay/relay_factory/topic_relay_factory.h"

// Include generated message processors
#for $pkg_name in $pkg_names
\#include "message_relay/processor/$(pkg_name)_message_processor.h"
#end for

namespace message_relay
{

TopicRelay::Ptr createTopicRelay(const TopicRelayParams &params)
{

  TopicRelay::Ptr relay;
  if (params.type.empty())
  {
    ROS_ERROR_STREAM("Must provide a topic relay type for topic " << params.topic);
  }
  #for $msg_name in $msg_srv_names
  else if (params.type == "$(msg_name)")
  {
    relay = TopicRelay::Ptr(new TopicRelayImpl<$(msg_name.replace('/', '::'))>(params));
  }
  #end for
  else
  {
    ROS_ERROR_STREAM("Unsupported topic relay type " << params.type);
  }
  return relay;

}

}
